{
  "name": "@blastdoor/target-stack",
  "version": "0.1.0",
  "description": "A deliberately breakable production estate with a deterministic fault injector, so the demo reproduces every run.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node src/server.ts"
  }
}
